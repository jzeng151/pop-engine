import type { ReactNode } from "react";

/** Route segment for live ops — composition in dashboard.css; fonts/canvas from root pe-shell. */
export default function DashboardLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
