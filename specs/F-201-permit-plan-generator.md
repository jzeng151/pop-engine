# F-201 · Permit Plan Generator

**Status:** APPROVED (2026-07-24; Acceptance Criterion 2's COVERAGE_GAP clause is amended 2026-07-27 under `docs/DOCUMENTATION-GOVERNANCE.md` §2 against the published legend, and is approved under §6 ("Regulatory source/status/content") by the product owner acting as verification owner and rules reviewer. ONE person signed in THREE capacities, all lanes being currently held by one person. §6 states two things about that, and the first is unconditional: "No person approves their own regulatory publication alone. The author and source reviewer should be distinct whenever the team size permits." The first sentence does not bite here because there is no regulatory publication to approve: the amendment asserts no new regulatory fact, changes no rule, trigger, threshold, deadline or verification status, and conforms a lower-authority artifact to the legend already published in `rules/nyc-rules.v2.8.json` under §2's authority hierarchy. The second sentence is the one that applies, and its "whenever the team size permits" is what a single-person team cannot satisfy. Recorded so the sole-approver fact is visible rather than implied; Outputs and Acceptance Criterion 1 amended 2026-08-08, product-owner approved, so a merged finding retains every contributing rule's published values and not only its id, see the second Amendment at the foot of this file) · **Reviewer/approver:** product owner · **Owner:** see Lane below · see `docs/BASELINE.md`.
**Phase:** 1 (core, week 1) · **Lane:** Dev 1 · **Depends on:** F-101, ruleset nyc.v2.11 ratified (BASELINE.md) · **Feeds:** F-102, F-202, F-203, F-204
**Updated:** 2026-07-22 against nyc.v2.1; retargeted through nyc.v2.8 for the changes recorded in `docs/BASELINE.md`, to nyc.v2.9 on 2026-07-29, to nyc.v2.10 the same day for issue #181's citation-only correction, and to nyc.v2.11 for organizer summaries. The shared issue #178 publication adds the nine issue #107 named confirmations, F-110's two active assembly-document fields, and issue #194's active food-claim removal. The registry changes add no finding or verdict; the named confirmations change no substantive requirement or verdict and narrow near-empty to plans with no `required` or `prohibited_or_ineligible` finding. v2.10 and v2.11 change no trigger, status, or verdict. Scenario B remains CONDITIONAL with low identified burden and is not near-empty because DOHMH-VENDOR-PERMIT-001 is `required + not_calculable`. Amended 2026-08-10 by product-owner direction in issue #275 so a missing-plan 404 renders stable organizer copy rather than the API's identifier-bearing error.

**Signed amendment:** one amendment is recorded at the end of this file (AC 12, the regeneration downgrade guard), APPROVED 2026-08-04 by the product owner in the product and architecture capacities. It is part of this spec's approved baseline, as is everything above and below it.

## User Story

As an independent organizer, I get the complete list of requirements my specific event triggers, with the agency, deadline, fee, portal, and either the official source the ruleset publishes or an explicit statement that the combination is not covered by this ruleset version, so I stop guessing what the city wants.

## Inputs

- An `events` row (F-101 fields) + its `revision_counter`.
- `rules/nyc-rules.v2.11.json` (authoritative, loaded in-memory at boot; AD-2/AD-9).
- `today` and the pinned holiday calendar (injected; the engine never reads the clock; AD-6/AD-11).

## Outputs

- One immutable `permit_plans` row (verdict via F-102, `ruleset_version`, `event_revision`, `intake_snapshot`) + `permit_plan_items` findings: kind (permit / insurance / notification / registration / eligibility / prohibition / dependency / advisory / note), disposition (required / may_be_required / prohibited_or_ineligible / advisory / no_new_requirement), agency, typed deadline + `latest_apply_date` + deadline_status, fee display, portal, complete source snapshots when published + verification status, every contributing rule ID, and the triggering answers. The sources snapshot is empty only for a source-less COVERAGE_GAP, which asserts nothing.
- `agency` is present on findings whose kind directs the organizer to act with a body (permit, insurance, notification, registration, eligibility, prohibition, dependency) and is boot-validated for those kinds. `advisory`, `note` and `classification` findings describe a condition rather than a filing and may carry none (#77); the engine never infers one from a rule ID or title.
- A finding's kind is the kind of the finding emitted, which equals the rule's kind for every rule except `classification`. A `classification` rule (SAPO-SCOPE-001 and the nine CONF-NO-* rules) persists as kind `note` with disposition `no_new_requirement`, keeping its rule ID in `rule_ids` for provenance; `classification` is never a persisted finding kind (#73).
- API: `POST /api/events/:id/plan` → plan + findings; `GET /api/events/:id/plan` → latest.

## Acceptance Criteria — General

1. Every finding references its rule ID and the intake answers that triggered it; dedupe-merged findings retain every contributing rule.
2. Every finding renders its verification status. A source-bearing finding renders its citation; a COVERAGE_GAP finding that carries no citation visibly states that the combination is not covered by this ruleset version, and never invents a citation or implies a source is merely missing (that is RESEARCH_REQUIRED's meaning; the published legend calls COVERAGE_GAP "combination not modeled by this ruleset version"). RESEARCH_REQUIRED renders "confirm with agency"; OFFICIAL_CONFLICT renders **both readings with both sources** (never silently resolved); COVERAGE_GAP advisories assert nothing.
3. Same event revision + same ruleset version + same `today` + same calendar → byte-identical **evaluation result** (verdict, finding set, and every computed date and status). Persistence identity is excluded: each generation is a new immutable `permit_plans` row, so `id` and `generated_at` differ by design (`ARCHITECTURE.md` permit_plans). Determinism is asserted on the engine output and the plan snapshot, not on the row's identity columns.
4. The near-empty result is first-class: when no finding has `disposition=required` or `disposition=prohibited_or_ineligible`, render "No definite city event requirement identified from your answers." plus every advisory and named confirmation. Deadline status is irrelevant: `required + not_calculable` is definite and never near-empty. Scenario B is a low-identified-burden case, not a near-empty case. Over-prescribing and overclaiming emptiness are both failure modes.
5. Rule-evaluation failure returns an explicit error; a partial plan is never presented as complete; a failure never yields a "no requirement" result.
6. Boot validation: the api refuses to start if the ruleset fails schema check (42 rules + 4 advisories, all trigger fields declared).

## Acceptance Criteria — Fixture Suite

**The acceptance suite lives in `docs/test-scenario-answer-key.md` (v7): six scenarios (A–F) plus the boundary/unit fixture list. It is derived from the ruleset and pinned to `today = 2026-07-22`. This spec deliberately does not duplicate the expected outputs — the fixture doc is the single copy.** Requirements:

7. All six scenarios pass: expected finding sets match exactly (kind + disposition + finding), 0 false omissions, 0 false additions, verdicts and rescope results match.
8. All boundary fixtures pass: park headcount 19/20/21; block party ± sales/ride; tent 399/400/401 sq ft; stage 2.0/2.5 ft × 119/120 sq ft; generator 2.5/2.6 gal; 39.9/40 kW; battery 20/20.1 kWh; street size unknown; other_sapo_class advisory; obstructs_public_way = no.
9. Scenario A's three rescopes are produced by full re-evaluation (size=medium → AT-RISK 5 days; size=small → AT-RISK on the DOHMH notification; private venue → SAPO + insurance drop), never by static text.
10. Scenario F's business-day computation counts actual business days (14 remaining vs 15 required) against the pinned calendar.
11. Scenario F's immutable `intake_snapshot` retains both F-110 answers as `unknown`; changing either answer produces a new event revision and snapshot while leaving its finding set and two material verdict branches unchanged. Historical food-exception claims remain inert in replay after issue #194 removes that field from active intake.

## Edge Cases

- OFFICIAL_CONFLICT rules (PARKS-TUA-001, PARKS-EVENT-EXACTLY-20-001) render as MAY_BE_REQUIRED with the conflict text; they never flip a verdict to INFEASIBLE on their own.
- Eligibility conflicts (block party + sales) render PROHIBITED_OR_INELIGIBLE with rescope guidance; the permit finding still lists so the user sees both.
- `research_required` deadlines are excluded from verdict/slack arithmetic but always listed.
- Uncollected/inapplicable branch fields evaluate per the registry's asked-when scoping; a field never asked is not a material unknown.

## Fixture Scenarios Exercised

All six + the boundary list, as the automated acceptance suite (Dev 1's lane gate and the green-gate criterion in DESIGN.md).

## Amendment (SIGNED 2026-08-04): regeneration downgrade guard

**Status: APPROVED 2026-08-04 by the product owner, and part of this spec's approved baseline.** AC 12 below adds behavior to an already-approved endpoint, so under `docs/DOCUMENTATION-GOVERNANCE.md` §6 it needs the **product owner's approval** (the "Product scope, feature meaning, phase" row, because it changes what `POST /api/events/:id/plan` does) and the **architecture owner's approval** (the "Durable architecture decision or dependency" row, because the guard adds a row lock and a transaction-ordering requirement to that endpoint's write path). Both were given on 2026-08-04 by the product owner, signing in exactly those two capacities: product, and architecture. ONE person signed in BOTH capacities, all lanes being currently held by one person, so this records one person's approval in two named capacities rather than two independent reviews. Recorded so the sole-approver fact is visible rather than implied. §6's "No person approves their own regulatory publication alone" does not bite here because there is no regulatory publication to approve: AC 12 asserts no regulatory fact and changes no rule, trigger, threshold, deadline, fee, portal or verification status. It refuses a write whose regulatory content cannot be shown to be at least as current as what the organizer was already given. The approval covers AC 12 and nothing else in this spec or any other. The decision record is in `docs/BASELINE.md` and `docs/OPEN-QUESTIONS.md` T-5 closes on it.

12. `POST /api/events/:id/plan` refuses to generate, and stores nothing, when the ruleset the service is running is older than the `ruleset_version` pinned by the latest stored plan for that event, or when the two versions cannot be ordered at all. Ordering is `nyc.vMAJOR.MINOR` compared as numbers, one implementation shared with the F-206 snapshot banner; an unparseable version and a version from another jurisdiction are unorderable and are refused, because nothing establishes that generating would not move the plan backwards. The comparison reads the latest stored plan inside the same transaction as the insert, serialized against a concurrent insert so a plan committed between the read and the write is seen. The refusal answers 409 and names both versions and which way round they stand, so a lane that has failed closed can be diagnosed from the response. An event with no stored plan generates on any version; an equal or newer version generates.

Why it belongs on the endpoint rather than in the browser: only the generating request sees both facts at once, the ruleset it is about to evaluate with and the plan it is about to supersede. A client check decides on reads that have already returned, and under RULES_FILE skew between deployments (#89) another deployment can store a newer-pinned plan in that interval, which no ordering of client reads observes.

## Amendment (SIGNED 2026-08-08): a merged finding retains every contributing rule's published values

**Status: APPROVED 2026-08-08 by the product owner, and part of this spec's approved baseline.** It amends Outputs and Acceptance Criterion 1 below, on two rows of `docs/DOCUMENTATION-GOVERNANCE.md` §6 at once: "Event Input, rules schema, OpenAPI, shared enum", because the plan response gains a per-finding structure and a new shared enum, and "Rule trigger, dedupe, branch, deadline, or formula semantics", because it changes which route the merged line's own values come from. Since the second-party review requirement was retired on 2026-08-05, the product owner's approval is the whole requirement for both, including where the product owner is also the author. The decision record is in `docs/BASELINE.md`; the durable-architecture obligation is met by the amendment to AD-19 in `docs/ARCHITECTURE-FUTURE.md` §2 on the same date. The design is `docs/proposals/dedupe-route-list.md` (APPROVED 2026-08-08).

**No regulatory fact is asserted and no ruleset is published.** No rule, trigger, deadline, fee, agency, threshold, portal, exception or verification status changes. Every value the route list carries is some contributing rule's own published value, and every contributing rule already appeared in `ruleIds` and `sources`.

- **Outputs, amended.** A finding produced by merging a `dedupe_key` group carries two further members. `routes` is the list of its contributing routes, one per contributing rule, each with that rule's own `ruleId`, trigger result (`true` or `unknown`, never `false`), the intake fields its own trigger could not resolve, disposition, name, agency, typed deadline, published deadline display, `latestApplyDate`, `applyAfterDate`, deadline status, slack days, fee display and the three portal fields. `headlineMode` says why the routes co-fired: `applies_together` where every contributing trigger resolved, `candidate` otherwise, computed as `resolved.length === routes.length`. Both are absent on an unmerged finding and on any plan stored before they existed, which is what keeps stored plans replaying unchanged (AD-7); `routes` is never an empty array, and the api and web validators refuse a response that makes it one. The merged line's own scalars come from ONE binding route, chosen from the routes contributing the merged disposition and narrowed to the resolved routes among them where any resolved route contributes it, so the line no longer names one rule and dates another.
- **Acceptance Criterion 1, amended.** "Dedupe-merged findings retain every contributing rule" is strengthened: they retain every contributing rule's own published VALUES, not only its id. A losing route's window, fee, portal and name are recoverable from the finding rather than destroyed by the merge, which is what lets F-102's window checks, F-202's checklist and F-203's reminders each read the route they are about.
- **What this does NOT change.** `ruleIds`, `notes`, `sources`, `triggeredBy`, `deadlineUnknownFields` and the summary points still concatenate in contributing order, so a plan is order-independent in every value the merge decides and not in the lists it retains. The losing routes' fee and portal are still not rendered on the merged LINE, because two fees or two portals on one line would read as two payments or two filings; they are rendered on their own route entry. No fixture, scenario or expected output in `docs/test-scenario-answer-key.md` is edited: Scenario E still renders one DOB line with both rule ids, `may_be_required` and `on_track`, and the deployed configuration still renders the tent permit's name, its `not_calculable` 15-business-day window, its TUP fee and its heading.

## Amendment (RECORDED 2026-08-08): the apply-by line for an undatable business-day window

**Status: APPROVED 2026-08-08 by the product owner, and part of this spec's approved baseline.** This amendment decides nothing. It carries the decision recorded in `docs/BASELINE.md` on 2026-08-08 (product owner, business-day apply-by copy) into the spec that owns what a plan line says about a finding's deadline, because `docs/DOCUMENTATION-GOVERNANCE.md` §5 step 6 requires the derived artifacts to be reconciled in the same change, and §7 puts a feature's behavior in its acceptance criteria rather than only in a decision record. Under §6 the change takes the "Regulatory source/status/content" row, which that record names and signs, and it reaches "Product scope, feature meaning, phase", whose required approval is the same product owner and is given in the same record, whose first sentence is about what the plan line says and not only about wording. Acceptance Criterion 13 states nothing the record does not: the same trigger, the same copy, the same single fallback. **One F-id, and it is this one.** Acceptance Criterion 4 already fixes organizer-facing plan-renderer copy and the deadline is this spec's own output, so the behavior belongs here; F-206 is untouched, since it owns the verification badge, the citation and the snapshot banner and this line carries none of them. **No contract changes.** No endpoint, field, enum, stored value or schema moves; the page reads the deadline's published `type`, which it already read, and `POST`/`GET /api/events/:id/plan` are unchanged. No regulatory fact moves either: the sentence states no permit, count, date, fee, holiday or agency practice, publishes no holiday list, changes no rule and changes no verdict, and SPEC-CONFLICT #130 stays open exactly as recorded.

13. A finding whose deadline could not be dated (`deadline_status = not_calculable`, no `latest_apply_date`) and whose published deadline `type` is `business_days_minimum` renders an apply-by line saying what the exact date depends on, rather than only that no date could be computed: "Apply by: the exact date depends on which days <agency> counts as business days. Allow more if it closes for holidays. Confirm with <agency>." The one substituted value is the finding's own published agency, carried from the plan and never inferred, and the line carries no count, no date, no holiday, no statement about what any agency publishes or observes, and no citation, because no located source defines the unit (`docs/VERIFICATION-SOURCES.md`) and a link beside a claim its page does not make is one an organizer can follow and find nothing. The published window itself stays on the deadline line above, with the qualification its source carries. Every plan carrying such a finding renders it, including a plan stored before organizer summaries existed, whose line has a different shape and the same sentence. A finding whose published agency does not read as the subject of that sentence renders the previous "not calculable, confirm with agency" line instead; that is DOB-ASSEMBLY-001 in nyc.v2.11 and no other rule, and correcting that rule's published agency string is a ruleset change this criterion does not make and does not approve. On a dedupe-merged finding the sentence names the merged `agency`, which the merge sources from a route that need not be the one that published the window (AD-19), so this criterion holds the published ruleset to the condition that makes the two the same answer: no dedupe group carrying a `business_days_minimum` deadline publishes more than one distinct agency across its contributing rules. A ruleset that broke that is a ruleset this line cannot be rendered from, and the check fails on the artifact rather than on the page.
14. A 404 from `GET /api/events/:id/plan` renders "No plan has been generated for this event yet." and never repeats an API error containing the event identifier. Other status codes retain their specific API error when present. Whether generation is offered still depends separately on the event read, so a missing event does not gain a generation action.
